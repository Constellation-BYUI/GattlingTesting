# GattlingTesting
To use this project, you will need Docker installed

Clone the project

From the command line cd to the new folder that was created for the project

Then type the following commands:
docker build -t gatling .
docker run gatling
